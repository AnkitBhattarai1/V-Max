
spring.application.name=USER-SERVICE
sever.port=0

spring.datasource.url= jdbc:mysql://${SQL_SERVER}:${SQL_SERVER_PORT}/user_service
spring.datasource.username=${MYSQL_USERNAME}
spring.datasource.password=${MYSQL_PASSWORD}

spring.jpa.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl.auto=update

spring.jpa.show.sql=true
spring.jpa.properties.hibernate.format_sql=true

#Mail configuration
spring.mail.host = smtp.gmail.com
spring.mail.port = 587
spring.mail.username=bhattaraiankit111@gmail.com
spring.mail.password=ipszxlrozjzmqxyg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


eureka.instance.prefer-ip-address=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
spring.cloud.loadbalancer.ribbon.enabled=false

eureka.instance.instance-id= ${spring.application.name}:${random.value}

#spring-data-redis
spring.redis.host=localhost
spring.redis.port=1637
spring.redis.databse=0
